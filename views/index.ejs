<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .title {
    text-align: center; /* Centers the text */
    font-size: 3rem; /* Makes the text larger */
    font-weight: bold; /* Makes the text bold */
    font-family: Arial, sans-serif; /* Sets a clean font */
    color: #ffffff; /* White text */
    background: linear-gradient(90deg, #007bff, #00c6ff); /* Gradient background */
    padding: 20px; /* Adds space around the text */
    border-radius: 10px; /* Rounds the corners */
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2); /* Adds a subtle shadow */
}

.weather-form {
    display: flex;
    justify-content: center;
    margin-top: 20px;
}

.input-group {
    display: flex;
    gap: 10px; /* Space between input and button */
    max-width: 600px;
}

.form-control {
    padding: 12px;
    font-size: 16px;
    border: 2px solid #007bff;
    border-radius: 8px;
    width: 250px;
    outline: none;
    transition: all 0.3s ease-in-out;
}

.form-control:focus {
    border-color: #0056b3;
    box-shadow: 0 0 8px rgba(0, 123, 255, 0.5);
}

.search-btn {
    padding: 12px 20px;
    font-size: 16px;
    border: none;
    background: #007bff;
    color: white;
    border-radius: 8px;
    cursor: pointer;
    transition: 0.3s;
}

.search-btn:hover {
    background: #0056b3;
}

/* Main container styling */
.main-container {
    text-align: center;
    background: linear-gradient(135deg, #007bff, #00c6ff);
    color: white;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    display: flex;
    justify-content: space-around;
    margin: 20px auto;
    max-width: 600px !important;
}

/* Location styling */
.location {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 5px;
}

/* Local time styling */
.local-time {
    font-size: 18px;
    opacity: 0.8;
    margin-bottom: 10px;
}

/* Temperature styling */
.temperature {
    font-size: 28px;
    font-weight: bold;
    margin-bottom: 10px;
}

/* Weather icon styling */
.weather-icon img {
    width: 80px;
    height: 80px;
    filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.2));
    
}

.textpart{
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.footer {
    text-align: center; /* Centers text */
    background: #222; /* Dark background */
    color: white; /* White text */
    padding: 15px; /* Adds spacing */
    font-size: 16px;
    font-weight: bold;
    border-top: 3px solid #007bff; /* Stylish top border */
    box-shadow: 0 -4px 10px rgba(0, 0, 0, 0.2); /* Adds a shadow effect */
    position: fixed; /* Keeps footer at the bottom */
    width: 100%; /* Full width */
    bottom: 0; /* Positions at the bottom */
}


    </style>
</head>
<body>
    <h1 class="title">Rapid Weather</h1>

   
    <form action="/getcity" method="POST" class="weather-form">
        <div class="input-group">
            <input type="text" name="place" class="form-control" placeholder="Enter city..." aria-label="City" required>
            <input type="submit" value="Search" class="search-btn">
        </div>
    </form>

    <% if(locals.time) {%>
        <div class="main-container">
            <div class="textpart">
                <p class="location"><%=location%></p>
            <p class="local-time"><%=time%></p>
            <p class="temperature"><%=temperature%>°</p>
            </div>
            <div class="weather-icon">
                <img src=<%=image%> alt="Weather Icon">
            </div>
        </div>
        <%} else if (locals.message) {%>
            <div class="main-container">
               <p><%=message%></p>
            </div>
        <%} else {%>
        <%}%>
        <footer class="footer">
            OGA © PRODUCTION
        </footer>
        
    
  
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>